#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <time.h>

int maxNUm = 10;

int main(void){
    int menuOpt = 3;
    int imput;
    int number;
    char menuRtn;
    time_t t;
    srand((unsigned) time(&t));

    while (menuOpt < 4 && menuOpt)

}